<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        AppUpdateKit internal activities.
        These are declared here so host apps don't need to add anything to their manifests.
        Both activities are fully blocking (no history, theme = translucent/full).
    -->

    <application>

        <!-- Force update blocking screen -->
        <activity
            android:name=".ForceUpdateActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:screenOrientation="portrait"
            android:excludeFromRecents="true" />

        <!-- Maintenance blocking screen -->
        <activity
            android:name=".MaintenanceActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:screenOrientation="portrait"
            android:excludeFromRecents="true" />

    </application>

</manifest>
